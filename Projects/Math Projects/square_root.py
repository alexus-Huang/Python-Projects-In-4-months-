# This file asks the user for the number they want to square root and returns the square rooted answer in the terminal
import math
question = float(input("Enter in a number you want to square root:"))
print(math.sqrt(question))
